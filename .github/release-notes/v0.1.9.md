## What's changed

- Fix the tray context-menu hierarchy so Desktop floating mode and Window attach mode are sibling display-mode options.
- Keep Top title bar and Bottom-right position choices in Appearance settings instead of presenting them as display modes.
- Remove the default WinForms background from rounded overlay antialiasing to prevent light edge artifacts, especially in dark themes on dark backgrounds.
